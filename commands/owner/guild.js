const Discord = require("discord.js");
module.exports = {
    name: "createguild",
    category: "owner",
    description: "Creates a server",
    run: async (client, message, args, channels) => {

    }
}